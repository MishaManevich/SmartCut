# Node dependencies (rebuilt from package.json)
node_modules/

# Whisper model (141MB — fetched by install-dev.sh on first run)
# HuggingFace mirror: https://huggingface.co/ggerganov/whisper.cpp
models/ggml-base.en.bin

# Build artifacts
dist/
.build-stage/
*.zxp

# Bundled transformers.js + wasm (regenerated by `npm run build:embedder`)
client/lib/transformers.bundle.js
bin/ort-wasm/ort-wasm-simd-threaded.jsep.wasm

# Signing material (never commit private keys or certs)
signing/
*.p12
*.pfx
*.key

# Local dev state and logs
.debug
*.log
debug.log

# OS junk
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
