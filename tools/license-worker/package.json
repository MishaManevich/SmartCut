{
  "name": "smartcutpro-license-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Cloudflare Worker: Paddle webhook ingest + license verify + signed download URL for SmartCut",
  "scripts": {
    "dev":     "wrangler dev",
    "deploy":  "wrangler deploy",
    "tail":    "wrangler tail",
    "secrets": "echo 'Run: wrangler secret put PADDLE_WEBHOOK_SECRET && wrangler secret put PADDLE_API_KEY && wrangler secret put DOWNLOAD_SIGNING_KEY && wrangler secret put ADMIN_TOKEN'"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
