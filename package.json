{
  "name": "smartcut-pro",
  "version": "1.0.0",
  "private": true,
  "description": "Build/dev tooling for SmartCut CEP extension. The extension itself is loaded directly from source by Premiere; this file only exists for bundling transformers.js and ORT WASM assets.",
  "scripts": {
    "build:embedder": "node tools/build-embedder.js",
    "fetch:model":    "node tools/fetch-model.js",
    "make-cert":      "./tools/make-cert.sh",
    "build:zxp":      "./tools/build-zxp.sh",
    "release":        "npm run build:zxp"
  },
  "devDependencies": {
    "@huggingface/transformers": "^3.0.2",
    "esbuild": "^0.24.0",
    "onnxruntime-web": "^1.19.2"
  }
}
